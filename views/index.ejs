<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/style.css">
    <title>TODO LIST</title>
</head>
<body>
    <style>
        ul{
            list-style-type: none;
            display: flexbox;
        }
    </style>

    <h1>TODO LIST</h1>
    <form class="row g-3" method="post" action="/submit">
        <div class="col-auto">
          <input type="text" class="form-control" id="inputPassword2" placeholder="New Task" name="title">
        </div>
        <div class="col-auto">
          <button type="submit" class="btn btn-primary mb-3">Add</button>
        </div>
      </form>





    <ul>
    <% if(locals.data) {%>
    <% data.forEach(list=> { %>
        <li>
                
                <form action="/edit" method="POST" style="display: inline;">
                    <input type="text" class="form-control" value="<%= list.title %>" aria-label="" name="title">
                    <input type="hidden" name="id" value="<%= list.id %>">
                    <button class="btn btn-outline-secondary" type="submit">Edit</button>
                </form>
                <form action="/delete" method="POST" style="display: inline;">
                    <input type="hidden" name="id" value="<%= list.id %>">
                    <button class="btn btn-outline-secondary" type="submit">Delete</button>
                </form>
            
        </li>
    <%   }); %>    
        <% }else{ %>
            <p>Add Data</p>
            <% } %>
        </ul>
</body>
</html>